# External ADK Samples Agent Catalog

Structured records live in `agent-catalog.yaml`.

This catalog indexes upstream Python sample agents from
`https://github.com/google/adk-samples/tree/main/python/agents`.

Use the YAML file as the source of truth for IDs, sources, summaries, tags, and
maturity. This Markdown file intentionally does not duplicate item records.
