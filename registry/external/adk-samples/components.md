# External ADK Samples Component Inventory

Structured records live in `component-inventory.yaml`.

This inventory records source-discovery counts for upstream ADK samples, such as
agent files, tool modules, skills, prompts, tests, evals, deployment assets, and
other reusable component indicators.

Use the YAML file as the source of truth. This Markdown file intentionally does
not duplicate item records.
