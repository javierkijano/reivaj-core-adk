# External ADK Skills Inventory

Structured records live in `skills.yaml`.

This inventory indexes ADK Skill resources discovered in upstream ADK samples,
including the `agent-skills-tutorial` and `youtube-analyst` sample skills.

Use the YAML file as the source of truth for skill sources, summaries, tags, and
maturity. This Markdown file intentionally does not duplicate item records.
