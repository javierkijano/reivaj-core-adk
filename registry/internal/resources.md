# Internal Resource Catalog

Structured records live in `resources.yaml`.

This catalog indexes repository-owned agents, workflows, skills, and reusable
knowledge resources. Internal `source` values are repository-relative paths.

Use the YAML file as the source of truth. This Markdown file intentionally does
not duplicate item records.
